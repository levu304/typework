{
  "name": "@typework/viewer",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only spreadsheet viewer (v1 of the OnlyOffice Document Server alternative)",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "probe": "node scripts/probe.cjs",
    "build": "tsc -p .",
    "test": "vitest run"
  },
  "dependencies": {
    "@levu304/excelrs": "^2.8.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5",
    "tsx": "^4",
    "vitest": "^3"
  }
}